export interface TrajectoryDisplayData {
    modelName: string;
    id: string;
    title: string;
    totalSimulatedTime: string;
    authors: string;
    publication: {
        title: string;
        url: string;
    };
    description: string;
}
